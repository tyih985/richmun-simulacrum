/**
 * Overseer: is a view-only type meant for adults who want to observe first
 * Staff: is the general student organizer and operators, with separate roles within
 * Delegate: is the term for all event participant attendees
 */
export type ParticipantType = 'admin' | 'overseer' | 'staff' | 'delegate';
