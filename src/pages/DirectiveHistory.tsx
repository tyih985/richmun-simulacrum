import { ReactElement } from "react"
import { Stack, Text } from '@mantine/core';

export const DirectiveHistory = (): ReactElement => {

    return (
        <Stack p="lg">
            <Text size="xl">Hello World</Text>
            
        </Stack>
    )
}